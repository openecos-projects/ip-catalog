# OpenECOS IP Catalog 首页改版需求文档

## 1. 项目目标

将当前基础表格页改造为面向芯片工程师的现代开源产品目录。新版沿用方案一的明亮、克制、工程化风格，提升 IP 检索、筛选和快速判断效率。

数据唯一来源为 `generated/index.json`。前端不得虚构或补录接口未提供的数据。

## 2. 页面结构

### 2.1 顶部导航

- 品牌名称：`OpenECOS IP Catalog`
- 导航项：探索 IP、贡献指南、项目文档、GitHub
- 操作项：中文/English、下载 JSON
- 贡献指南和项目文档使用 GitHub 仓库中的对应文件 URL，避免在 GitHub Pages 上使用不可达的相对路径。

### 2.2 首屏检索区

- 主标题：开源芯片 IP，随时可供评估
- 搜索框支持匹配 `uid`、`display_name`、`slug`、`summary`、`summary_zh`、`ip_family`、`interfaces`
- 输入后即时更新结果；清空输入恢复全部记录

### 2.3 数据概览

仅展示以下四项：

1. IP 总数：读取顶层 `total`
2. 分类数：对 `records[].category` 去重计数
3. 已流片验证：统计 `maturity === "silicon-proven"`
4. 候选条目：统计 `status === "candidate"`

### 2.4 目录工作区

左侧为筛选栏，右侧为结果区。支持卡片和表格两种视图，默认使用卡片视图。

筛选项及字段来源：

- 分类：`category`
- 功能族：`ip_family`
- 接口：`interfaces`
- 成熟度：`maturity`
- 许可证风险：`license_risk`
- 集成难度：`integration_difficulty`
- 数据质量：`data_quality`

筛选项从当前记录动态聚合，不预设不存在的选项。多组筛选之间使用 AND，同组多选使用 OR。结果区显示匹配数量、已选条件、清除筛选，以及按名称、UID、最后审核日期排序。

## 3. IP 卡片

每张卡片只能显示以下内容：

- 名称：`display_name`
- 标识：`uid`
- 分类和功能族：`category`、`ip_family`
- 接口标签：`interfaces`，默认最多显示 3 项，其余显示数量
- 成熟度：`maturity`
- 许可证风险：`license_risk`
- 摘要：中文使用 `summary_zh`，英文使用 `summary`
- 底部信息：`languages`、`license`、`last_reviewed_at`

点击卡片打开站内详情抽屉，不直接离开目录。卡片不得显示本节白名单之外的数据。

## 4. 表格视图

表格列为：

`display_name`、`uid`、`category`、`interfaces`、`maturity`、`license`、`license_risk`、`integration_difficulty`、`last_reviewed_at`。

表头支持排序；长接口列表截断展示；名称列在横向滚动时固定。

## 5. 详情抽屉

详情按以下分组显示，并隐藏空值：

- 基本信息：`uid`、`display_name`、`slug`、`summary_zh`/`summary`、`category`、`subcategories`、`ip_family`
- 技术能力：`languages`、`interfaces`、`bus_compatibility`、`compatibility`、`features`、`implementation_style`
- 集成评估：`integration_profile`、`resource_profile`、`performance_profile`、`integration_difficulty`、`best_for`、`not_recommended_for`
- 验证与版本：`maturity`、`fpga_support`、`asic_support`、`current_ref`、`current_ref_type`
- 法务信息：`license`、`license_risk`、`commercial_use_allowed`
- 数据状态：`status`、`data_quality`、`documentation_quality`、`last_reviewed_at`、`next_action`
- 来源链接：`catalog_repository`、`upstream_repository`、`homepage`、`documentation`

外部链接必须使用数据中的原始 URL，并在新标签页打开。`path` 仅用于定位仓库中的 YAML 记录。

`commercial_use_allowed` 需要兼容布尔值和字符串值：布尔值显示“是/否”，字符串值按原值映射；不得使用普通真假判断吞掉 `"unknown"`。

## 6. 视觉规范

- 使用白色和浅蓝灰背景、深海军蓝正文、青绿色主色、蓝色辅助色
- 风险色：`low` 使用绿色、`medium` 使用橙色、`high` 使用红色
- 采用 8px 圆角、轻边框和极弱阴影，避免大面积渐变
- 标题突出，正文紧凑；标签、状态和链接保持清晰的视觉层级
- 电路走线纹理只作为低对比度背景装饰，不承载信息

## 7. 响应式与可用性

- 桌面端：左侧固定筛选栏，右侧双列卡片
- 平板端：筛选栏折叠，卡片改为单列
- 手机端：顶部导航收起，筛选通过抽屉打开，默认使用卡片视图
- 所有操作支持键盘访问，并提供清晰焦点状态
- 状态不能只依赖颜色表达，必须同时显示文本
- 中英文切换后，摘要和静态标签同步更新
- 翻译表缺少某个枚举值时显示原始值，不得隐藏该记录

## 8. 数据约束与验收

- 页面只读取 `generated/index.json` 和 `site/i18n/value-labels.json`
- 可渲染字段严格限定为本文第 2～5 节列出的 JSON 字段；不得构造白名单之外的业务属性
- 缺失值显示“未知”或隐藏，不得推测
- 概览总数必须与顶层 `total` 一致，筛选结果数必须由当前加载的 `records` 计算
- 现有 22 条记录均可渲染，空数组和空字符串不会导致布局异常
- 卡片、表格和详情对同一字段使用一致的值与翻译
- JSON 加载失败时显示明确错误信息，并保留重试入口
